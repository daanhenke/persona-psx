/* Persona 1 (JP) - NAME's first word.  NAME @ 0x800643A0.
 *
 * Every overlay starts with its own number, ahead of its code; the name
 * screen is overlay 9.
 */
#include <decomp/types.h>

const u_long g_overlay_base = 9;
