
/* resolved from the main link */

/* resolved from the main link */

/* The overlays' entry points, which main's overlay table names. Only C
   reaches them, so splat does not put them in its undefined list. */
ovl_name_entry = 0x800643A4;
ovl_dng_entry = 0x80064D2C;
ovl_casino_entry = 0x80065DAC;
ovl_adv_entry = 0x8007D9EC;
ovl_btlp_entry = 0x8007F408;
ovl_s2d_entry = 0x80089C04;

/* PreloadDng reads the save floor signed through this second name (reloc.main.txt). */
g_save_floor = 0x801F15A0;
